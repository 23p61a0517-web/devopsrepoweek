pipeline {
    agent any
    stages {
        stage('Clone') {
            steps {
                 clone
            }
        }

        stage('Build') {
            steps {
                echo 'Build running'
            }
        }
    }
}
